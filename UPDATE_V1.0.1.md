# UPDATE v1.0.1 — 动力学与制导全量规范（2026-08）

- 基线：v1.0.0 冻结层（2026-08-14，见 `V1_FREEZE.md`，本更新不改动冻结路径 `configs/aim120a_h2.yaml` / `direct_fin_g` 语义）
- 适用范围：profile 运行时（`plant_model: critical_damped_v12`，即 `plant_semantics: fin_torque_body_aoa`）
- 证据来源：datamine 字段直读、Dagor 引擎公开接口、三次游戏回放遥测（PL-12 快/慢发射、R-77 慢发射，共 121 帧数字化，其中 59 帧进入升力联合拟合）
- 测试状态：228 通过 / 4 失败（4 个失败均为缺 `data/raw/statshark_h4` 数据文件的预存问题，与模型无关）

---

## 0. 本版本相对 v1.0.0 的变化概要

| # | 变化 | 依据 |
|---|---|---|
| 1 | 阻力从 "CxK 当绝对 Cd × 高斯马赫形状" 改为 **Cxi 形状因子机制**：CdA₀ = S_w·CxK·C_et(M)，C_et 为全弹种共享标准曲线 | gameparams `useCxiMach:true`；引擎存在 `calc_cxi(M)` 函数；PL-12 35 km 遥测反推 5 个 CdA 锚点（±1.6%） |
| 2 | 攻角诱导阻力面积基改为**纯弹径面积**（不乘 wingAreaMult） | gameparams `applyWingAreaMultToCxAoA:false` |
| 3 | 横向力改为**打包升力、α 键控、线性律**：斜率 = 0.58·(finsLatAccel/finsAoa)·η_q，无肘点无饱和（η∈[0.55,1.7] 实测区间内） | 三次回放 59 帧联合拟合，扣除推力横向分量后 R²=0.97~0.998 |
| 4 | `loadFactorMax` 导入并作为**输出过载径向硬帽**（仅作用于打包升力） | datamine 字段；2.57 版 AIM-54 调参 diff 证实其"最大过载"语义 |
| 5 | `distFromCmToStab` 重新解读为**静稳裕度**（CM→全弹中性点距离），作为风向标刚度臂 | 108 弹量纲核验（0.03~1.48 倍弹径，中位 0.65）；AIM-54 大翼近中立 0.03 倍自洽 |
| 6 | 姿态改为**四元数**积分（legacy 植体），转动方程 I·ω̇ = K(δ−α) − Cω，ζ=1 临界闭合 | 运动学一致性修复；游戏姿态内核闭源，为声明代理 |
| 7 | 制导比力指令加**重力补偿**，reqAccelMax 帽**后置**于补偿之后 | 控制基准一致性；AIM-54 diff 证实 reqAccelMax=公告"最大过载" |
| 8 | timeToHitGain 查表用 **t_go = R/max(Vc, \|V_rel\|)**，杜绝侧射几何 t_go 虚高触发自削增益死循环 | 90° 侧射指令压制解剖（增益 0.81→0.68 恶性循环实测） |
| 9 | 自动驾驶仪恢复 **v12 语义：datamine accelControl P/I/D 原值 → 舵角（rad）**，误差单位为 g | AAM-4(积分主导)/Derby(弱积分) 中远距排序体感复现；m/s² 单位假设被否决（见 §7） |
| 10 | 新增**中段 PIP 甩头**（发射初段前置转弯，候选机制） | 回放导引头 IOG+DL→TRK 相位结构 + 近满迎角甩头实测；真实弹三段式制导标配 |
| 11 | 缺 `baseIndSpeed` 的 profile 回退共享 1800 km/h（不再静默禁用 q 缩放） | 全库 113 弹缺字段；现版本空空弹均为 1800 |

---

## 1. 模型总览

平动为质点、转动为 CM–NP 单杆的 5-DOF 伪六自由度（无滚转，skid-to-turn）。

```
状态  x = ( r⃗, v⃗, Q, ω_p, ω_y, m, δ_p, δ_y )
```

惯性系 x–z 水平、y 向上；体轴基 (f̂, û, r̂) 由单位四元数 Q 给出；ω_p 绕 r̂（俯仰）、ω_y 绕 −û（偏航）；δ 为舵角状态（行程 = finsAoa）。

积分：定步长 RK4（0.02 s；sensor_track 1/256 s），控制量步间零阶保持，推进段边界显式分步，事件（引信/落地/寿命）步内插值。

## 2. 大气与流场量

```
ISA: ρ(h), a(h);   v⃗_air = v⃗ − w⃗,  V = |v⃗_air|,  M = V/a,  q = ½ρV²
q_base = ½ρ₀·(baseIndSpeed/3.6)²                （PL-12: 153 kPa）
η_q = min( q/q_base, 4 )                         η_cap=4 为数值保护，实测区间内不触发

α_p = atan2(−v̂·û, v̂·f̂),  α_y = atan2(−v̂·r̂, v̂·f̂),  α = √(α_p²+α_y²)
n̂_p, n̂_y = û, r̂ 对 v̂ 的正交化流法向基
```

游戏"IAS"经载机读数核验为 EAS（TAS·√σ，实测比值 0.703 vs ISA 理论 0.702）。

## 3. 推进（多脉冲）

```
段 i: (F_i, τ_i, Δm_i):  T(t)=F_i,  ṁ=−Δm_i/τ_i;  燃尽后 T=0, m=m_end
F⃗_T = T(t)·f̂
```

已声明差距：2.57 起游戏推力随外界气压增大。6.8 km 回放反推续航段隐含推力 **+16%**（14.9±3.2 kN vs datamine 12.88 kN），未建模，记入差异清单。

## 4. 阻力 — Cxi 形状因子机制

datamine `CxK` 是乘在引擎内置标准弹阻力定律 `calc_cxi(M)` 上的形状因子（俄式弹道学 Cx = i·Cx_эт(M) 约定），**不是绝对阻力系数**。机制证据：gameparams `shellBallisticsParams.useCxiMach: true`；DagorEngine 公开桩文件中存在 `calc_cx(float)` / `calc_cxi(float)`；导弹走 shell 弹道求解器族（`ballistics::simulate(ShellEnv, ShellProps, ShellState, dt, t)`，火箭电机为该模块子结构）。

```
CdA₀(M) = S_w·CxK·C_et(M)          S_w = (πd²/4)·wingAreaMult
CdA_α  = S_d·CxAoA·min(α, 1.2)²    S_d = πd²/4（不乘 wingMult）, CxAoA = 9（gameparams 全局）
CdA_δ  = S_w·c_δ·(δ_p²+δ_y²)       可选，缺省 0
F⃗_D = −q·(CdA₀ + CdA_α + CdA_δ)·v̂
```

标准曲线 C_et(M)（全弹种共享，线性插值；M1.93–3.10 为 PL-12 35 km 遥测锚定，其余为苏式 1943 型定律形状 ×1.10）：

```
M    0.8   0.9   1.0   1.1   1.2   1.4   1.6   1.8  |1.93  2.01  2.29  2.66  3.10| 3.5   4.0
Cet .173  .209  .330  .402  .413  .396  .380  .358  |.352  .342  .314  .288  .270| .250  .231
                                shape ————————————— |———————— DATA ——————————————| shape
```

历史注记：v1.0.0 时代的 0.2995 "AIM-120A 拟合缩放" 即是 C_et 在 M2–3 段数值的经验近似，本机制落地后正式退役。

## 5. 横向力 — 打包升力，α 键控，线性律

AAM 无 CyK 字段；`finsLatAccel` 是全弹（体+翼+尾）升力打包，`finsAoa` 是最大可用迎角（2.57 版 AIM-54 调参 diff 与官方公告逐字对应：loadFactorMax/reqAccelMax 17→22 = "最大过载"，finsAoa 0.218→0.436 rad = "最大可用迎角 12.5°→25°"）。

```
(u_p, u_y) = disk( α_p/α_max , α_y/α_max )        |u|>1 径向缩回单位圆（=失速平台）
F⃗_N = m·g₀·[ k_s·A·η_q ]·( u_p·n̂_p + u_y·n̂_y )·s_cap
k_s = packed_lift_slope_scale = 0.58
s_cap = min( 1, n_max·g₀ / |F⃗_N/m| )              ← loadFactorMax 输出硬帽（仅作用于 F_N）
```

- 三次回放联合拟合：扣除推力横向分量 T·sinα/(m·g) 后，气动斜率 k = 1.173 / 1.080 / 1.134 g/°/η（PL-12 快 / PL-12 慢 / R-77），R² = 0.998 / 0.984 / 0.971；k ÷ 各弹 (A/α_max) 聚在 0.56~0.59 → 取共享 0.58。
- **无肘点无饱和**：η ∈ [0.55, 1.7] 实测区间内严格线性。loadFactorMax 仅作为独立输出帽保留（实测未触及，语义独立成立）。
- 屏显过载已证实**包含推力横向分量**；模型经物理自动包含（推力沿弹轴投影），无需显式项。
- 单帧端到端复现：PL-12 慢弹 t=1.5 s 帧（1990 km/h, α=15°, 助推段）模型 15.9 G vs 屏显 15.0 G。
- 0.58 的按弹缩放 vs 全局常数**尚未判别**（PL-12 与 R-77 的 A/α_max 仅差 5%）；AAM-4 为天然判官（预测：按弹 0.88 g/°/η，全局 1.13）。

## 6. 转动 — CM–NP 单杆

`distFromCmToStab` = 静稳裕度 Δ（CM 到全弹中性点距离）。量纲核验：108 弹全部落在 0.03~1.48 倍弹径（中位 0.65）；AIM-54 大中翼近中立 0.03 倍、AMRAAM 小翼 0.98 倍，设计模式自洽；由此导出的风向标频率 2.2（凤凰）~25 rad/s（格斗弹）全部物理合理。

```
I  = m·L²/12                        （均匀杆代理）
N′ = ∂N/∂α = m·g₀·k_s·A·η_q/α_max   （与 §5 平动共用同一升力斜率——两本账一个数）
K  = N′·Δ
I·ω̇_k = K·(δ_k − α_k) − C·ω_k,  C = 2ζ√(KI), ζ = 1（临界闭合，代理）
ω_n = √( k_s·A·g₀·η_q·Δ / ((L²/12)·α_max) )       质量无关
配平: α_trim = δ ⇒ 过载 = k_s·A·η_q·δ/α_max
```

升力作用于中性点（管力矩）；阻力沿 −v̂ 过 CM 不生力矩；舵力真实尾臂被打包进 K（δ 与 α 共用刚度，比值定配平）。姿态运动学：Q̇ = ½·Q⊗(0, ω_p·r̂ − ω_y·û)。

## 7. 制导与控制 — 三段式

### 7.1 中段 PIP 甩头（候选机制，本版新增）

回放证据：导引头 IOG+DL（惯导+数据链）阶段即以近满迎角甩头（R-77 慢发射 α 骑 20~24°/上限 26.4°），0.8~0.9 s 转 TRK。对应真实弹标准三段式（AMRAAM/R-77 的 INS+数据链中段 → 主动末段）；甩头任务 = 把预测遭遇点转进导引头框架角（datamine `radarSeeker.angleMax=55°`）。datamine 无对应增益字段，参数由回放标定。

```
PIP 解算: |R⃗ + V⃗_t·t| = V̄·t 的最小正根 t̂,  PIP = 目标位置 + 目标速度·t̂,  V̄ = max(V, 200)
甩头指令: ε = ∠(v̂, PIP方向),  a⃗_mc = (V·ε/τ_turn)·ê,  与 PN/loft 相加后统一钳位
权重:     w=1 (t<0.8s) → 线性淡出至 0 (t=1.3s);  ε∝指令 使其自然自熄
参数:     τ_turn=0.8 s（对 PL-12 慢弹 5 点扫描标定，SSE 7.4）, lock=0.8, blend=0.5
```

标定成绩：30° 慢发射 α 曲线全程 ±2°（峰值 15.7°@1.40 s vs 游戏 15.2°@1.4 s）；40° statshark R-77 找回近炸；90° R-77 从 6056 m 拉回 68 m。

### 7.2 末段 PN + loft

```
a⃗_PN = N_pn·G(t, t_go)·V_c·(Ω⃗×v̂)          N_pn=propNavMult（datamine）
t_go = R / max(Vc, |V_rel|)                 ← 本版修复：侧射不再虚高
G = flightTimeGain(t)·timeToHitGain(t_go)   （datamine 表直读）
loft: 俯仰角误差×20 g/rad, 垂直分量        （datamine loftElevation 等直读）
指令帽: |a⃗_c| ≤ reqAccelMax·g₀
比力指令: f⃗_c = a⃗_c − g⃗（法向投影），再帽 |f⃗_c| ≤ reqAccelMax·g₀   ← 重力补偿后置帽
```

### 7.3 自动驾驶仪（v12 语义）

```
误差 e = 指令比力 − 体轴测量比力（单位: g）
δ_cmd = P·e + I·∫e + D·ė                    （datamine accelControlProp/Intg/Diff 原值, 输出=舵角 rad）
钳位: |δ| ≤ finsAoa, (δ_p, δ_y) 成对圆盘;  舵机一阶 τ_a=0.08 s
```

按弹差异化实测有效：AAM-4（I=0.0445 积分主导）低动压端点兑现率高于 Derby（I=0.0065），复现游戏"AAM-4 手感 ≥ Derby"的中远距排序。

## 8. 参数出处总表（示例值 = PL-12）

| 参数 | 值 | 出处等级 |
|---|---|---|
| d, L, m₀, m_end, wingAreaMult, 推进段 | 0.203 / 3.85 / 198 / 160 / 1.4 / 34960N×2.5s+12880N×5s | datamine 直读 |
| CxK | 1.6 | datamine 直读（Cxi 形状因子语义：引擎证据） |
| C_et(M) | §4 表 | 遥测锚定（M1.9–3.1）+ 1943 律形状 |
| CxAoA=9、面积基规则 | — | gameparams 直读 |
| A=finsLatAccel, α_max=finsAoa | 41.4 g / 0.375 rad | datamine 直读（官方语义经 AIM-54 diff 证实） |
| baseIndSpeed | 1800 km/h | datamine 直读（缺失回退共享 1800） |
| Δ=distFromCmToStab | 0.12 m (0.59d) | datamine 直读（静稳裕度解读，全库量纲核验） |
| n_max=loadFactorMax, n_req=reqAccelMax | 38 / 38 | datamine 直读 |
| accelControl P/I/D | 0.0181/0.013/0.00025 | datamine 直读（v12 舵角语义） |
| PN 增益与调度表、loft 参数 | 4.0 + 表 | datamine 直读 |
| **k_s = 0.58** | — | **遥测拟合**（59 帧联合，本版唯一新拟合常数） |
| 甩头 τ_turn/lock/blend | 0.8/0.8/0.5 s | 回放标定（候选机制） |
| I=mL²/12, ζ=1, τ_a=0.08, η_cap=4 | — | 声明代理 |

## 9. 实测锚点（验证证据）

| 锚点 | 模型 | 游戏 |
|---|---|---|
| 35 km 对头弹道 | 33.26 s / 487 m/s / 8845 m / 命中 | 32.77 s / 557 / 9229 / 命中 |
| 30° 快弹 G–α（t=3.2/4.2/5.1 s） | 14.9/8.6/1.2 G | 15.5/7.1/1.1 G |
| 30° 慢弹 α 曲线（5 点） | ±2° 以内，峰值 15.7°@1.40 s | 峰值 15.2°@1.4 s |
| 单帧复现（慢弹 t=1.5 s） | 15.9 G | 15.0 G |
| 发射疲软（慢速发射峰值 G） | 低速折扣 η² 直接给出 | 15.0 G ✓ |
| R-77 低速折扣形式 | ∝(IAS/1800)² | 相邻帧比值检验逐段吻合 |
| statshark 40°/8 km | 双弹近炸 | （参考数据）双弹近炸 |

## 10. 已否决假设存档（防止重蹈）

| 假设 | 否决证据 |
|---|---|
| CxK 为绝对 Cd（0.2995 缩放时代） | useCxiMach 机制 + 遥测 CdA 反推（修正比 0.27~0.35 随 M 变化） |
| PID 误差单位 m/s²（×9.8） | 继电器极限环 ±4.8G@5.1Hz；舵机越快越糟；dt 加密 4 倍震幅不变——三连击 |
| 升力斜率饱和于 loadFactorMax | 19 帧数值巧合（助推→续航推力项与 η 增长对消）；R-77+慢弹数据线性穿过"肘点" |
| η_q 高侧持续放大权限（cap=4） | 59 帧拟合：0.58 线性律，高侧无额外增长 |
| distFromCmToStab 为尾舵力臂 | AIM-54 值 0.0125 m 对 4 m 弹荒谬；静稳裕度解读全库自洽 |
| 共享外环级联（丢弃每弹 PID） | AAM-4/Derby 排序与游戏手感相反；v12 原值恢复后复现 |

## 11. 已声明简化与已知差距

1. **R-77 IOG 段 α 平台短**（模型 ~15° vs 游戏 20~24°）：甩头指令需穿过 R-77 冷 PID 才成舵角；游戏 IOG 段疑似直接姿态指令绕过 accelControl。下一个候选改动：IOG 段直接舵角路由。
2. **推力-高度机制未建模**（+16% @6.8 km 已测量，函数形式待高空发射数据）。
3. **k_s=0.58 按弹 vs 全局未判别**（AAM-4 30° 回放三帧可定案：0.88 vs 1.13）。
4. C_et 在 M<1.9 / M>3.1 为形状外推；loadFactorMax 输出帽未被数据触及；τ_turn 顶在标定区间上界（0.8，最优可能 ~1.0）；70° PL-12 开甩头后 min_dist 反而 45→89 m 的小异常已记录未究。
5. 无滚转；CG 不移动；I 为均匀杆、ζ=1（可用离轴发射摆动周期辨识）；舵机无速率限制；甩头为候选机制（无 datamine 字段）。
6. 大迎角非线性以 §5 圆盘为游戏化替身；末段回放帧 G/α 偏高疑似屏显 α 为单平面分量（待垂直机动帧判别）。

---

*本文件与 `config/profile_h2_runtime_defaults.json` 的 boundary 字符串、`profile_adapter.py` 的 assumptions 共同构成本版本的语义边界声明。数值均为独立本地工程候选；公开可见的名称与参数不构成与私有实现等价的断言。*
